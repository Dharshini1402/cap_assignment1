Project #5: Case Changer

public class Demo {
   public static void main(String []args){
      char c = 0,d=0,e=0;
     String uppercase="",lowercase="",capitalize="",sentencecase="";
      String str = "Mary has a little dog. she calls him Pun";
        System.out.println("Input: "+str);
      // length of string
      int len = str.length();
      StringBuffer strBuffer = new StringBuffer(len);
      StringBuffer strBuffer1 = new StringBuffer(len);
      StringBuffer strBuffer2 = new StringBuffer(len);
      for (int i = 0; i < len; i++) {
         c = str.charAt(i);
         // title case converted to lower case
         if (Character.isTitleCase(c)) {
            c = Character.toLowerCase(c);
         }
         // upper case converted to lower case
         if (Character.isUpperCase(c)) {
            c = Character.toLowerCase(c);
         }
         // lower case converted to upper case
         if (Character.isLowerCase(c)) {
             c = Character.toUpperCase(c);
         }
       strBuffer.append(c);
       uppercase = strBuffer.toString();
      }
      System.out.println("Upper case: "+uppercase);
       for (int j = 0; j < len; j++) {
         d = str.charAt(j);
         // title case converted to lower case
         if (Character.isTitleCase(d)) {
            d = Character.toLowerCase(d);
         }
         // upper case converted to lower case
         if (Character.isUpperCase(d)) {
            d = Character.toLowerCase(d);
         }
       strBuffer1.append(d);
       lowercase = strBuffer1.toString();
      }
      
      System.out.println("Lower case: "+lowercase);
    
        String words[]=str.split("\\s");
        for(String word:words)
        {
            // Capitalize first letter
            String firstLetter=word.substring(0,1);
            // Get remaining letter
            String remainingLetters=word.substring(1);
            capitalize+=firstLetter.toUpperCase()+remainingLetters+" ";
        }
      System.out.println("Capitalize: "+capitalize);
      
     
       // Print the first character as it is
        System.out.print("Sentence case: " +str.charAt(0));
     
        // Traverse the rest of the
        // characters one by one
        int i = 1;
        while (i < str.length())
        {
     
            // If current character is uppercase
            // print space followed by the
            // current character in lowercase
            if (str.charAt(i) >= 'A' && str.charAt(i) <= 'Z')
                System.out.print( " "+ Character.toLowerCase(str.charAt(i)));
     
            // Else print the current character
            else
                System.out.print(str.charAt(i));
                 
            i++;
        }
       
      
   }
}